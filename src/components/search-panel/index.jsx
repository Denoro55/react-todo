import React from "react";

const SearchPanel = () => {
    const placeholderText = 'Search';

    return <input className="form-control input" placeholder={placeholderText} type="text"/>
};

export default SearchPanel;
